import { ServerDetail } from '../types';

export const INITIAL_SERVERS: ServerDetail[] = [
  {
    id: 'server-alpha',
    name: 'server-alpha',
    ip: '10.0.1.10',
    env: 'PRODUCTION',
    status: 'degraded',
    os: 'Ubuntu 22.04.3 LTS',
    region: 'jakarta-idc',
    uptime: '4h',
    cpuUsagePercent: 17,
    memoryUsedBytes: 30_236_569_763, // 88% of 32 GB
    memoryTotalBytes: 34_359_738_368, // 32 GB
    diskUsedBytes: 240_000_000_000,
    diskTotalBytes: 536_870_912_000,
    networkInBytesPerSec: 42_000_000,
    networkOutBytesPerSec: 98_000_000,
    hostedServices: ['identity'],
    maxCapacity: 3,
    tier: 'critical',
    allowedStacks: ['go', 'nodejs'],
    complianceStatus: 'warning',
    ruleViolations: ['Peringatan Resource: Host memori mencapai 88% (mendekati batas aman 85%)'],
    processesCount: 142,
    kernelVersion: 'Linux 5.15.0-88-generic x86_64',
    cpuHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(15 + Math.sin(i * 0.5) * 4),
    })),
    memoryHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: 88,
    })),
    diskIopsHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      read: 1100,
      write: 750,
    })),
    networkHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      in: 42,
      out: 98,
    })),
  },
  {
    id: 'server-beta',
    name: 'server-beta',
    ip: '10.0.1.20',
    env: 'PRODUCTION',
    status: 'critical',
    os: 'Ubuntu 22.04.3 LTS',
    region: 'jakarta-idc',
    uptime: '4h',
    cpuUsagePercent: 17,
    memoryUsedBytes: 30_236_569_763, // 88% of 32 GB
    memoryTotalBytes: 34_359_738_368, // 32 GB
    diskUsedBytes: 320_000_000_000,
    diskTotalBytes: 536_870_912_000,
    networkInBytesPerSec: 12_000_000,
    networkOutBytesPerSec: 18_000_000,
    hostedServices: [],
    maxCapacity: 4,
    tier: 'standard',
    allowedStacks: ['nodejs'],
    complianceStatus: 'violation',
    ruleViolations: ['Ambang Batas Kritis: Host berstatus Critical dengan utilisasi RAM 88%. Penempatan service baru dilarang.'],
    processesCount: 88,
    kernelVersion: 'Linux 5.15.0-88-generic x86_64',
    cpuHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(15 + Math.sin(i * 0.3) * 3),
    })),
    memoryHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: 88,
    })),
    diskIopsHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      read: 850,
      write: 400,
    })),
    networkHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      in: 12,
      out: 18,
    })),
  },
  {
    id: 'prod-web-01',
    name: 'prod-web-01',
    ip: '10.0.1.30',
    env: 'PRODUCTION',
    status: 'healthy',
    os: 'Ubuntu 22.04.3 LTS',
    region: 'jakarta-idc',
    uptime: '42d 6h',
    cpuUsagePercent: 42,
    memoryUsedBytes: 20_959_440_404, // 61% of 32 GB
    memoryTotalBytes: 34_359_738_368, // 32 GB
    diskUsedBytes: 225_485_783_040,
    diskTotalBytes: 536_870_912_000,
    networkInBytesPerSec: 58_400_000,
    networkOutBytesPerSec: 142_800_000,
    hostedServices: ['audit', 'health-profile'],
    maxCapacity: 4,
    tier: 'standard',
    allowedStacks: ['go', 'nodejs'],
    complianceStatus: 'compliant',
    processesCount: 142,
    kernelVersion: 'Linux 5.15.0-88-generic x86_64',
    cpuHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(38 + Math.sin(i * 0.5) * 6 + (i % 2)),
    })),
    memoryHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(61 + Math.cos(i * 0.4) * 2),
    })),
    diskIopsHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      read: Math.round(1200 + Math.sin(i) * 300),
      write: Math.round(800 + Math.cos(i) * 200),
    })),
    networkHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      in: Math.round(45 + Math.sin(i * 0.6) * 15),
      out: Math.round(110 + Math.cos(i * 0.6) * 30),
    })),
  },
  {
    id: 'prod-web-02',
    name: 'prod-web-02',
    ip: '10.0.1.20',
    env: 'PRODUCTION',
    status: 'degraded',
    os: 'Ubuntu 22.04.3 LTS',
    region: 'jakarta-idc',
    uptime: '18d 2h',
    cpuUsagePercent: 78,
    memoryUsedBytes: 28_862_180_229, // 84% of 32 GB
    memoryTotalBytes: 34_359_738_368, // 32 GB
    diskUsedBytes: 310_273_528_320,
    diskTotalBytes: 536_870_912_000,
    networkInBytesPerSec: 135_000_000,
    networkOutBytesPerSec: 210_000_000,
    hostedServices: ['live-consult', 'lifestyle', 'medical-record'],
    maxCapacity: 4,
    tier: 'standard',
    allowedStacks: ['nodejs'],
    complianceStatus: 'compliant',
    processesCount: 264,
    kernelVersion: 'Linux 5.15.0-88-generic x86_64',
    cpuHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.min(95, Math.round(72 + i * 0.4 + Math.sin(i) * 6)),
    })),
    memoryHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.min(90, Math.round(82 + (i % 3))),
    })),
    diskIopsHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      read: Math.round(4800 + Math.sin(i) * 900),
      write: Math.round(3900 + Math.cos(i) * 700),
    })),
    networkHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      in: Math.round(115 + Math.sin(i * 0.4) * 20),
      out: Math.round(195 + Math.cos(i * 0.4) * 25),
    })),
  },
  {
    id: 'staging-web-01',
    name: 'staging-web-01',
    ip: '10.0.2.10',
    env: 'STAGING',
    status: 'healthy',
    os: 'Ubuntu 22.04.3 LTS',
    region: 'surabaya-dr',
    uptime: '12d 9h',
    cpuUsagePercent: 23,
    memoryUsedBytes: 13_056_700_580, // 38% of 32 GB
    memoryTotalBytes: 34_359_738_368, // 32 GB
    diskUsedBytes: 120_000_000_000,
    diskTotalBytes: 536_870_912_000,
    networkInBytesPerSec: 15_000_000,
    networkOutBytesPerSec: 22_000_000,
    hostedServices: ['audit'],
    maxCapacity: 4,
    tier: 'internal',
    allowedStacks: ['go', 'nodejs'],
    complianceStatus: 'compliant',
    processesCount: 92,
    kernelVersion: 'Linux 5.15.0-88-generic x86_64',
    cpuHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(20 + Math.sin(i * 0.4) * 5),
    })),
    memoryHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(38 + (i % 2)),
    })),
    diskIopsHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      read: Math.round(400 + Math.sin(i) * 100),
      write: Math.round(250 + Math.cos(i) * 50),
    })),
    networkHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      in: Math.round(12 + Math.sin(i * 0.5) * 4),
      out: Math.round(18 + Math.cos(i * 0.5) * 5),
    })),
  },
  {
    id: 'prod-db-01',
    name: 'prod-db-01',
    ip: '10.0.1.50',
    env: 'PRODUCTION',
    status: 'healthy',
    os: 'Ubuntu 22.04.3 LTS',
    region: 'jakarta-idc',
    uptime: '64d 1h',
    cpuUsagePercent: 55,
    memoryUsedBytes: 49_478_023_270, // 72% of 64 GB
    memoryTotalBytes: 68_719_476_736, // 64 GB
    diskUsedBytes: 620_993_459_200,
    diskTotalBytes: 2_199_023_255_552,
    networkInBytesPerSec: 92_000_000,
    networkOutBytesPerSec: 88_000_000,
    hostedServices: [],
    maxCapacity: 4,
    tier: 'critical',
    allowedStacks: ['go', 'nodejs'],
    complianceStatus: 'compliant',
    processesCount: 312,
    kernelVersion: 'Linux 5.15.0-88-generic x86_64',
    cpuHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(52 + Math.sin(i * 0.5) * 8),
    })),
    memoryHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      value: Math.round(72 + Math.sin(i * 0.2) * 2),
    })),
    diskIopsHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      read: Math.round(5800 + Math.sin(i) * 1100),
      write: Math.round(4200 + Math.cos(i) * 800),
    })),
    networkHistory: Array.from({ length: 24 }, (_, i) => ({
      timestamp: `${i}:00`,
      in: Math.round(88 + Math.sin(i * 0.5) * 15),
      out: Math.round(84 + Math.cos(i * 0.5) * 12),
    })),
  },
];
